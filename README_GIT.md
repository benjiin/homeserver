# Status & Überblick
git status
git log
git diff

# Änderungen speichern
git add .
git add dateiname.py
git commit -m "Nachricht"

# Mit GitHub synchronisieren
git push origin main
git pull origin main

# Repo klonen
git clone URL

# Virtuelle Umgebung nicht vergessen vorher
source venv/bin/activate